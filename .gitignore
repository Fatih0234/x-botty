.DS_Store
.env
# config.json is committed — it has no secrets (cookies/keys are in GH secrets)
cookies.json
reports/
# data/*.db is intentionally tracked — committed back by GitHub Actions after each run
__pycache__/
*.pyc
.venv/
